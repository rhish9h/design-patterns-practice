package adapter;

public class Rectangle {
    public void draw(int x, int y, int width, int height) {
        System.out.println("Rectangle drawn with coordinates: " + x + " : " + y +
                " width, height = " + width + " : " + height);
    }
}
